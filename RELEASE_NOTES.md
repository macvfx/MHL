# Release Notes

One line per release, newest first. Detailed changes live in each app's own documentation
([MHL Verify changelog](MHL_VERIFY_CHANGELOG.md), per-app READMEs and user guides).

## Media Trust Tools suite (CopyTrust, Drop Verify, Folder Copy Compare, mhl-tool)

| Version | Date | Summary |
|---|---|---|
| **CopyTrust · Drop Verify · Folder Copy Compare 2.7.8.6 Build 13** *(BETA)* | 2026-09-01 | **The enforced-naming card row now says what it will do, and its fields are in the order the folder name is written.** The Files control read "Follow preset", which names where the renaming decision comes from and never what it is — and opening the menu repeated the same words. It reads **Rename Files Yes** in green or **No** in red, resolved exactly as the copy resolves it: this card's answer, then the convention, then the session-wide switch. The menu still offers all three choices, and a card whose rule requires renaming shows a locked state instead of the empty control it used to render. The naming fields were fixed at unit, roll, location for every facility while the preview beneath them read a different order; they now follow the convention's own template, so reading the row left to right is reading the folder name. The Unit, Location and Roll menus each drew a chevron on top of the system's, which read as a doubled arrow. Nothing about the copy changed. The version moves rather than only the build so that deployment tools, which compare the version string, see an update at all. |
| **CopyTrust · Drop Verify · Folder Copy Compare 2.7.8.5 Build 12** *(BETA)* | 2026-08-31 | **Three faults from one facility session log, and anyone using an enforced preset with staged destinations should take this build.** From the **second launch onwards the destinations never staged**: a preset's settings are written into preferences and are already in force at launch, but its destinations are session state and survive nothing, so restoring the preset restored only its identity — leaving the name in the toolbar, the convention enforced, and nothing staged. They had to be loaded by hand every morning, and under an enforced preset the Load item is deliberately absent. There is now also a **Stage This Preset's Destinations Again** item that works under the lock, since staging what the pinned preset already names changes nothing about which preset is in force. **One missing volume asked three times** — three things notice an enforced preset at launch and the guard meant to stop the second and third was reading a value the first had not finished writing; a redeployed preset still applies at once. And **a missing dailies share no longer asks whether you are off site**: that question is about the storage the job copies to, and a proxies-only destination's absence stops nothing, since every original still lands on every destination that takes originals. Staging still reports it. |
| **CopyTrust · Drop Verify · Folder Copy Compare 2.7.8.4 Build 11** *(BETA)* | 2026-08-30 | **Four things a day of testing found.** The worst was silent: **Settings ▸ Edit Enforced Naming… emptied a preset's destinations.** It opened the wizard without telling it about the destinations staged in the main window, so the wizard said "nothing is staged" to a facility with three staged — and saved that, taking the Archive-to-P5 and Create-proxies roles with them. It now says it cannot see the session and keeps what the preset carries; it also edits the loaded preset rather than creating a near-duplicate on every save. **Settings for the copy mode you are not in could not be changed**: the Folder Copy tab's exclusion checkboxes sprang back, and a change on Card Copy then appeared to reach Folder — the edit saved correctly and the window never redrew, so the Folder tab displayed its own state only when something else forced a redraw. The two modes have always been stored separately. **The Unit menu remembered every keystroke** — S, So, Son, Sony — because a unit was recorded as it was typed; it is recorded on finish now, the fragments are cleared once, and there is a Forget item. **The wizard warns as well as blocks**: a convention's warnings had been computed since the feature shipped and shown nowhere, and the first new one catches a folder template using `{location}` while locations are switched off — which silently drops that part of every folder name and shows no field to fill it. |
| **CopyTrust · Drop Verify · Folder Copy Compare 2.7.8.3 Build 10** *(BETA)* | 2026-08-30 | **A facility can lock a preset in.** Once a preset has been tested and agreed it is the configuration — naming convention, what each destination is for, the P5 server, every copy setting — and there was no way to hold a fleet on one: an operator could load a different preset or none, and the only evidence afterwards was the name in the receipt. A configuration profile now pins **which** preset by name, and nothing else; the preset's content stays a `.json` in `/Users/Shared/CopyTrust/Presets`, deployed however the app itself is. That split is the point — if the profile carried the convention, every changed folder template during testing would mean re-signing and re-pushing a profile and waiting for each Mac, so iterating would be impossible. CopyTrust also **watches that folder now**, which it had always claimed to do and never did: a preset replaced there is in force within a second or two on a running app, where before it took effect at the next launch. Under `enforce` the Preset button carries a lock and loading, editing and saving are unavailable — Export and Save Report… stay, because reading out what is in force is not what a lock protects against. `seed` applies once on a Mac that has never loaded a preset. It fixes which preset is in force, not every individual setting; a changed setting shows the modified dot and is put back at the next launch. Setting it up: [CopyTrust_ManagedPresetDeployment.md](CopyTrust_ManagedPresetDeployment.md). |
| **CopyTrust · Drop Verify · Folder Copy Compare 2.7.8.2 Build 9** *(BETA)* | 2026-08-30 | **The right menus, and one export instead of four.** The five-step panel that opens on a new build pointed at menus that do not hold what it named: Check for Updates is in the CopyTrust menu under About, not Help, and Help has no What's New item of its own — it has CopyTrust Help, with What's New as a topic inside that window. Its **All release notes** link opened the GitHub releases page and now opens CopyTrust Help at What's New, because the panel appears on a machine that has just been updated, often on a network that will not reach github.com, and what the reader wants is this build's changes. Underneath all three was a real defect: two sheets on one window cannot both be open, so asking for one from inside the other did nothing — Help's *Open the What's New panel* button had been dead since it was added. **Preset ▸ Save Report… gains a fourth option**: all three documents and the preset `.json` into one folder, because a facility handing a job over wants all of them and the `.json` is the only one of the four that can be loaded on the next Mac. The exported preset is the file CopyTrust itself would save, and carries no password. The version moves rather than only the build because Munki ignores build numbers, so a build 9 of 2.7.8.1 would reach no managed Mac. |
| **CopyTrust · Drop Verify · Folder Copy Compare 2.7.8.1 Build 8** *(BETA)* | 2026-08-29 | **Where evidence lands, and what a preset can say.** A delivery came out with two receipts folders: the EXIF metadata CSV was still writing to the pre-2.7.7 `CopyTrust_Receipts` while everything else went to `Receipts`, so the folder you open looking for the metadata export was the one without it in. Underneath it, half the writers resolved that folder through one routine and half appended the name themselves — agreeing on a fresh delivery and disagreeing on every folder an older build had written into. There is one resolver now. **Nothing is written at the top of a destination drive any more**: the session receipt and the per-copy log were going to the folder staged in the Destinations panel, usually a volume root, while every other artifact went into the delivered folder — so every job left a receipts folder at the root of every drive as well as the real one beside the footage. They now follow the card into the folder it landed in, including under enforced naming where that is inside the project folder. **A preset can say what each destination is for** — Archive to P5 and Create proxies, per destination, so one drive can be the archive source while another makes the proxies; CopyTrust always carried this and the only place to set it was the destination row, where Create proxies is on for everything you add. **A preset carries the P5 server** — address, port, API version, user, archive index, client and plan — so loading it on another Mac leaves only the password, which is never in a preset file and stays in each operator's Keychain. **The exported report describes the whole preset** rather than the naming half. **The panel that opens on launch is now the order of work** — newest build, load the preset, P5 password, choose sources, assign project, Start — with what changed moved to Help ▸ What's New. The version gains a fourth number because Munki compares the version string and ignores the build number, so every build of 2.7.8 looked to it like the same release. |
| **CopyTrust · Drop Verify · Folder Copy Compare 2.7.8 Build 7** *(BETA)* | 2026-08-28 | **CopyTrust checks footage against the manifest it arrived with.** A card shot off site is usually offloaded by somebody else — OffShoot, Silverstack, ShotPut Pro — and reaches the facility as a folder with its own MHL beside it. CopyTrust ingested that folder and never looked at the manifest, so a file that was **already corrupt when it arrived** was copied and verified without complaint: CopyTrust hashes what it reads and what it writes, and those agree, because it faithfully copied a broken file. The receipt said clean, proxies were made from bad footage, and the shuttle drive was wiped; the damage surfaced weeks later in an edit with nothing left to say whether it arrived that way. **A staged card now says what it came with and who wrote it**, before anything is copied — and says so either way, because an absent manifest and one nobody looked for are otherwise indistinguishable. **The check itself happens during the copy at no extra cost**, reusing the hashes the copy already takes off the source as it reads it; it needs Inline or Full verification, and under Quick the row says so rather than promising a check that cannot happen. **The result is kept apart from CopyTrust's own verification**, in a Source Provenance card above Session Health, because they are different claims by different authors: a card that arrived damaged copies and verifies perfectly, and only the arrival check can say otherwise. A mismatch never blocks a copy — damaged footage is still the only footage, and the drive it came on is usually about to be wiped. **A manifest CopyTrust wrote gives no arrival verdict**: checking footage against our own record only confirms it still matches what we recorded. Authorship is read from inside the manifest rather than from its filename, so a rename does not change the answer. **Green means everything was checked** — a partly-checked manifest is amber and says how much, because one matching entry among fifty unreadable ones is not a card that matched. A manifest that cannot be parsed is reported as unreadable, never as absent. **Also:** each finished card now reports *copied & verified* at roughly twice the size of anything else on screen, above the artifact rows, because that is what an operator needs on walking back to the machine; artifacts fold away under each card, closed by default; a convention built in the wizard creates the project folder for a new job instead of falling back to a dated ingest folder; and Drop Verify and Folder Copy Compare no longer scan the `Receipts` folder they write into, so a second Drop Verify pass does not hash its own output and a deep compare of a clean delivery reports no differences. **Known limits:** individual manifests are read, an ASC MHL *history* is not — `ascmhl_chain.xml`, manifest C4 identifiers and multiple generations are not parsed, so "the chain was checked" is not a claim this build makes. CopyTrust still writes MHL v1.1. `xxh3`, `xxh128` and `c4` are parsed but not computed, and entries using them are counted and reported rather than skipped silently. Test notes: [TEST_NOTES_v2.7.8.md](TEST_NOTES_v2.7.8.md). |
| **CopyTrust · Drop Verify · Folder Copy Compare 2.7.7 Build 2** *(public beta)* | 2026-08-26 | **Enforced naming, in beta and wanting testing.** A facility can describe how it files footage — project, unit, roll, and the folders each destination expects — and CopyTrust applies it as cards are staged. **One preset now sets up a whole job:** the ingest options, the naming convention, and the destinations, which travel with the preset as a volume name and a path rather than as machine-specific bookmark data, so a preset written at one facility means the same thing on another Mac. A volume that is not mounted is requested from SMB Connect; CopyTrust still never mounts and never holds credentials. **Every card shows where it will land before the copy starts,** and receipts and manifests record what each card was assigned — project, unit, roll, whether the roll was proposed or set by hand, the preset that imposed the convention, and the exact folder each destination received. A convention can describe two differently-laid-out drives in one job. **Fixed: losing a network volume mid-session froze the app.** With a receipt export folder on a share, resolving that folder let macOS try to remount the missing share through a call with no timeout, and the app waited on it while merely drawing a menu — twenty seconds unresponsive on a client Mac. A folder on a share that has gone now reads as absent instead of stalling; reconnecting a share stays the operator's job, in Finder. **Also:** filename clashes between cards are caught before the copy rather than in an edit weeks later, and only the cards causing a clash are proposed for renaming; preflight totals exactly what the copy will move instead of counting files the exclusions would drop; a capture register at each destination root records every ingest; and a card pulled from the reader before Start is reported as a failure instead of a successful copy of nothing. **Build 9 fixes two faults found in site testing.** The project list could empty and stay empty: a scan that found nothing replaced the list already loaded, and nothing rescanned afterwards, so correcting the convention or staging a card appeared to do nothing for the rest of the session — a scan that finds nothing now keeps the previous list and says so. And a delivered folder name using `{date_compact}` read roll numbers as if no card had ever been filed, proposing **roll 1** for every card, silently, on the template the client-site script recommends; the token is now understood by the roll reader and offered in the naming controls rather than existing only in the renderer. **Build 10 makes building the convention the workable part.** Setting one up for a real client was the hardest thing in the feature, so it is now done by pointing rather than typing: pick a project the facility already has and CopyTrust offers what that folder could stand for — this year, or every year — with the number of projects each would list. A root inside a year folder lists that year alone and stops finding anything when the year turns over, which used to happen silently. Footage and proxy folders are picked the same way, with the project's own number written back as a token so the answer describes every future job. The volume field is gone: a convention describes how storage is laid out, and which drive it lands on is the destination you stage — the launch check now asks whether *those* are mounted rather than volumes named in a field the project list ignores. Adds an optional **location** folder level that disappears when a card has none, `{project_name}` and `{project_full}` for folders that carry more than a number, and a new convention that starts as guardrails: every field asked for, none blocking, until you decide otherwise. **Build 11 fixes the signed convention report**, which omitted the location field entirely — a facility that asked operators where a card was shot would have signed a document that never mentioned it — and rewrites the blank decision sheet, the form agreed before setup, for how the app now works. **Builds 12 and 13 close nine defects a review of the naming feature found, four of them release-gating and every one of them silent** — the app said one thing and the copy did another, or a decision an operator made left no trace. File renaming now does what the card says, and each card has its own Rename / Do not rename control; check the filenames on the destination, because the row was right before and the copy was not. A roll marked **Unknown** survives into the folder name instead of disappearing from it, and a second unknown-roll card of the same project and unit is filed as `UnknownRoll_2` rather than joining the first card's folder — checked against the folders already on the drive, not only the cards staged now. A convention that cannot work is refused at Save with the reason given, and one that arrives from an older export no longer blocks every card: copying carries on unenforced behind a banner saying the convention is not being applied. A `..` typed as a unit or a location can no longer move a card out of the folder its preview showed. The location an operator records now reaches the session log, the text receipt and the JSON receipt. Correcting a delivery folder asks which card it belongs to, so a wrong path on the second or third staged card can be fixed rather than only looked at. A project folder that cannot be created sends the card to the dated ingest folder the planning chose instead of the top of the drive. Saving a convention in the wizard now loads it, so a receipt names the convention that made the folders rather than the one before it. **And a project picked once fills in the cards staged after it,** marked *carried from this session* and changeable card by card — four cards from one shoot day used to mean four identical selections. Build 12 was never released; build 13 carries all nine. **Builds 14 to 16 are what running build 13 on real storage found, and the first of them is the largest thing in this release.** The half of enforced naming that files a card **inside its project folder** had no control anywhere in the app, and had not had one since 2.7.4 Build 16, when the preset generator became the single editor for the convention: the naming switch moved across and the destination switch did not. Every preset the wizard could write named folders correctly and then left the card at the top of the drive. The resolver underneath was complete and correct the whole time — nothing ever called it. **Put the card inside its project folder** is now a switch in step 4 of the wizard, and with it on a card lands in `Projects/2026/2026-014 Name/…` on each destination, as the preview has always said it would. **Which root holds a project is now asked of the disk.** Storage with a root per year had every job looked for under whichever root happened to exist first, so a 2025 job was searched for under 2026 — and, with creation allowed, a second folder for it was written there while its real folder sat one directory away. Every root is searched, whichever already holds the project wins, and only a project no root holds is created, under the year in its own number. Two roots holding one project is reported rather than chosen between. **A preset can be edited.** The wizard could only ever create, so changing one switch meant inventing a version name and keeping two files that differed by a boolean; **Edit “<name>”…** now updates a preset in place, and renaming while editing is a rename rather than a fork. **Smaller things from the same session:** the chosen project is spelled out under the card's picker, which could squeeze its own selection down to an ellipsis; the per-card renaming control is labelled **Files** and says what each option does to them; and the Camera Card exclusion warning appears once instead of twice and offers **Open Settings…**, so an operator who disagrees with it can reach the setting rather than hunt for it. **A queue left running overnight no longer stops on a dialogue nobody is there to answer.** Auto-advance, queued runs and the resume that fires when a drive reappears reached two confirmations that were unconditional modals; they are recorded now instead. The one exception is a project found under two folder names, which CopyTrust has never decided for itself: the run stops at that card with everything before it finished and verified, names both folder names, and notifies. **Builds 17 and 18 are what a second Mac and a facility's own production data found.** **Proxies are now filed like everything else:** a proxies-only destination resolved nothing at all, copying straight to the top of its volume, so a drive laid out `Projects/2026/…` like every other destination received proxies at its root — and it was invisible to the project list, because nothing had ever needed to scan it. It resolves its own project folder now, with the same creation and fallback behaviour as a copy destination. **A preset can be imported**, not only exported — the way back in was previously moving a file into Application Support by hand — and two presets sharing a name are told apart in the menu by when they were last saved. **The wizard previews the folder path, not just the folder name**: `1n {project}/Footage` is shown expanded, rendered by the same code the copy uses, so what the braces become is visible before a client's storage is committed to it. **Fixed:** *folder will be created* was read as *your project was not found* — an existing project with a new `Footage` level beneath it said exactly what a missing project said, and the row now distinguishes them. **Also:** the project list can be saved as a CSV, one row per project per destination; and *Show project list…* and *Show capture register…* no longer sit one line apart in the same colour, answering questions asked at opposite ends of a job. **Build 18 rewrites the in-app What's New panel**, which opens by itself after an update and was pinned five builds back — describing cards landing in project folders in a build where the switch for that did not exist. It is five lines now. In-app Help gains the placement switch, root selection, proxy filing, the per-card **Files** control, preset editing and importing, and the note that *Card* is the copy mode rather than the shape of the source: a card copied off site by another tool arrives as a folder and is still a card. **Builds 19 to 29 are two days of a facility running this on their own storage, and they divide in two.** Four were faults that would have cost someone footage or a day. **A token typed with a bracket instead of a brace was written to a NAS as a literal folder** — `1n [project}` — because the renderer matches token names exactly and brackets are legal in a filename; token-shaped text that is not a token is now refused, with the fix named. **Picking a project could hang the window for fifteen seconds**: working out where a card would land re-listed every destination on every redraw, which on a network volume is thousands of directory entries per frame. **A proxies-only destination filed into `Footage`** rather than the convention's proxy path — the one destination that exists purely for proxies was putting them where the originals go. **And a P5 archive was handed paths at the top of the volume** for files that live inside the project folder, so P5 answered with a server error that read as its own fault and was not. **The rest are what using it turns up.** *Check delivery folder…* is a window listing every card against every destination with the volume in bold, rather than an alert; the path list under a card folds away until wanted. **A P5 archive that failed can be re-sent** with *Try P5 Again*, using current settings and copying nothing. **Renaming that is required now has to have something to rename with** — the prefix lives in Settings and an empty one silently renamed nothing — and the field is editable whether or not the session switch is on. **`{project_full}` is accepted** where the folder name must carry the project; it was rejected for not containing `{project}`, which it does not, being `{project_full}`. **Cards can carry a shoot date** for footage ingested days after it was shot, **units you type are remembered** and offered next time, **six tokens the renderer always accepted are now offered** in the prefix builder, and **destination rows say what kind of drive they are** — network, SAN, removable or internal — rather than all wearing the same grey glyph. Both *Volumes* buttons are gone and *Destination Sets* has moved into the preset menu. **With enforced naming switched off, nothing about a copy, its receipt or its log changes.** **Build 30 is one fault wearing different clothes: files skipped silently, on a copy that finished, verified clean and reported a count — of what it copied rather than of what was there.** Seven exclusion patterns were switched on for you and nobody chose them: two MHL manifest rules, `receipt_`, the two receipt folders and the two proxy folders. **All seven are off now, in both modes**, still offered so anything you want skipped is a checkbox away, and the rows that can match files *you* made say so. Installing this build switches them off once and records that it has; switch one back on and it stays on. **Two of them were never CopyTrust's files.** `Final Cut Proxy Media` is Final Cut Pro's own folder name, and a pattern matches a folder anywhere in a tree and takes everything beneath it — so pointing CopyTrust at a proxy tree copied **nothing** and reported a clean finish, dropping proxies a facility transcoded themselves exactly as it dropped CopyTrust's own. `receipt_` is a plain prefix, so a folder holding `receipt_camera_rental.pdf` lost it. Proxies are still never transcoded twice; that check moved into the proxy generator instead of being a side effect of the files never arriving. **A manifest that arrives with a source now arrives with the copy.** A card offloaded elsewhere — ShotPut Pro, Hedge, OffShoot, Silverstack or the camera — reaches a facility with an MHL beside it, and that manifest is the only record of what was on the card; it was being left behind on a drive usually about to be wiped, so *did the footage match what the offload tool said it was?* stopped being answerable by anyone rather than merely going unasked. An ASC MHL source was worse: the `ascmhl/` folder arrived carrying only its chain file, an index whose every referenced manifest was missing — a chain that looks present and is hollow. It arrives whole now. Exclusions distinguish **who wrote a manifest** rather than which mode is running, so a re-copy can leave a stale CopyTrust manifest behind while still keeping the one another tool wrote. Receipts travel too: archiving a delivered folder to a second drive carries its `CopyTrust_Receipts` instead of stripping the record of where that footage came from. CopyTrust does not yet *verify* footage against an incoming manifest — this build keeps the evidence; reading it is planned for 2.8.0. **And a P5 archive contains the proxies**, where it had listed the proxy receipts — a document naming every proxy by path — and none of the proxy files. They are marked `CT_KIND proxy` so they are never mistaken for camera media, and scoped to the destination being archived, so a proxies-only delivery on another volume stays out of it. Every fault here was silent, so testing it means **counting what arrived** rather than watching a run. **Build 31 adds two things testing build 30 turned up.** A naming convention is enforced from the next launch, but the Presets button had no way to find the preset again — so it read “Preset”, nothing in the list was marked, and Edit and Update were absent, while the banner enforced a convention it would not name. **The button now shows the preset's name, the list ticks it, and the banner says which preset set the delivery folder.** And the exclusion groups say what they hold: *File Storage* had become a drawer holding desktop clutter, a copy's evidence and the proxy folder names under one heading, so a single All/None toggle carried three unrelated consequences. The groups are now **Receipts & Manifests**, **Proxies**, and **System** — where `.DS_Store` and `Thumbs.db` belong — with Drop Verify using the same names. Checkbox states are unchanged; only the heading a row appears under has moved. **2.7.6 build 1 is 2.7.5 build 32 renumbered**, so it installs cleanly over whichever 2.7.5 a Mac is carrying — 2.7.5 reached testers at builds 29, 30 and 31 in turn. No functional difference. It also fixes the in-app Help, where eight topics shared one segmented control and were truncated rather than wrapped, and where three statements had stopped being true — including the claim that presets do not carry destinations, which they have since 2.7.5. **2.7.7 moves all three apps together and settles two rules: a copy never omits a file without being told to, and nothing stops an operator working.** **One Receipts folder, shared.** CopyTrust wrote `CopyTrust_Receipts` and Drop Verify wrote `Drop Verify_Receipts`, so a folder both had touched carried two — neither name saying what was inside, only which application had been run. Both write a visible **`Receipts`** folder inside the delivery now, each app's files named as its own; all three names are still read, so an older delivery stays readable and a second copy joins the folder already there. The volume-root register — *what was ingested here and never filed against a project* — is hidden as **`.Receipts`**, an admin's question rather than a row in every Finder window, with the old `_CopyTrust_Index` still read. Exported receipts get their own `Receipts` folder inside the one you chose. **Nothing blocks a copy.** A missing project volume raised a modal alert at launch, holding up an operator who might not be copying to that volume at all — and its third button, the way out, was pushed past the alert's own edge by the width of the other two: drawn, and not clickable. The banner already on screen carries the same three choices as ordinary buttons — **Try Again**, **Open SMB Connect**, **Off Site…** — and Open SMB Connect now names the volume it is waiting on. **A read-only destination is refused**, at preflight and at the moment you choose it: a mounted disk image reports POSIX-writable while every write fails, so a destination nothing could be written to used to pass. **And the app names the preset it is enforcing** — on the button, ticked in the list, and in the banner — where a convention could previously be in force with nothing able to say which preset it came from. **Build 2 fixes three things found testing build 1 at a facility.** A P5 job running longer than a minute looked *silent* to the session's stall watchdog, which P5's own progress had never fed — so ending the session cancelled the monitor, and that cancellation was written as a **failed archive** for work P5 went on to complete. It records **submitted** now, naming the job ID, and a live archive counts as live however recently it last spoke; P5's word `failure`, which a *running* job reports throughout, is read as a verdict only once the job has stopped. **A clip and its own camera sidecar were reported as a filename conflict** — the proxy audit compares basenames with the extension removed, which is right for proxies, but it was handed every file on the card, and every professional camera writes `A001C001.XML` beside `A001C001.MXF`. So a card holding one clip conflicted with its own metadata, which fired on essentially every real card; only files that will actually be transcoded can claim a proxy name now, and the audit does not run when no destination is making proxies. **And a read-only destination is refused** at the moment it is chosen as well as at preflight. **All three apps ship at 2.7.7 Build 2.** |
| **CopyTrust 2.7.4 Build 27 / Drop Verify 2.7.4 Build 27 / Folder Copy Compare 2.7.4 Build 27** *(public beta)* | 2026-08-11 | **Drop Verify MHLs now document the complete included camera card, not only recognized media.** XML clip metadata, camera databases, support files and thumbnail folders are hashed alongside picture and sound; contact sheets, metadata CSVs and proxies remain media-focused. **Camera Card exclusions cannot operate silently:** CopyTrust and Drop Verify show a mandatory, non-suppressible confirmation naming every active Camera Card pattern before a copy/hash begins, because `THMBNL`, `CLIPINF` or another match can remove a complete structural folder from the copy/MHL. Cancelling writes nothing. Canonical component-based relative paths also prevent macOS path aliases such as `/var` and `/private/var` from corrupting MHL paths or defeating exclusion matching. |
| **CopyTrust 2.7.3 Build 1 / Drop Verify 2.7.3 Build 1 / Folder Copy Compare 2.7.3 Build 1** *(public beta)* | 2026-08-09 | **CopyTrust could quit unexpectedly at End Session.** It happened when a session was ended, when it was ended with artifacts skipped, or as the last artifact finished — a view that rebuilt itself without end until the app ran out of stack. Copies and verification had always completed before it struck, so nothing was lost, but the session had to be reopened. It accounts for every crash report from this line. **The window now belongs to the panels that do the work:** Sources and Destinations take the height left after the sections below them rather than a fixed band at the top, with drop zones that grow to match, and Available Volumes keeps the bottom. **A running copy leads with a progress panel** — percentage, speed, time remaining, a bar per destination, and files listed as they are copied and verified — instead of the activity log, which now stays closed unless you open it or turn it back on in Settings → Post-Copy → During Copy. The run holds the window until End Session rather than snapping back to empty drop zones as the last file lands. **Fixed:** the file list only ever filled under inline verification; quick and full verification now report each file too. **Fixed:** a P5 notice could not be dismissed and stopped a copy from starting at all — notices are read, not ticked, and are written to the session log, because a missed P5 selection is archived afterwards and should never hold up a copy. Archive to P5 on a destination is disabled while P5 archiving is off in Settings, where it had no effect. **Fixed:** receipts and MHL files recorded a name supplied by the network — on some connections the internet provider's name for the connection — instead of this Mac. They now record the computer name from Sharing; files already written are unchanged. **Session Summary** drops the monospaced receipt block that repeated the sections above it: copy speed joins the summary metrics, build, host, operator and session move to a line at the foot, and Copy Receipt still copies the full text. **All three apps move together at 2.7.3 Build 1**, closing 2.7.2 at CopyTrust Build 40. |
| **CopyTrust 2.7.2 Build 33 / Drop Verify 2.7.2 Build 16** *(public beta)* | 2026-08-09 | **The `tree` setting is gone from both apps.** Settings → External Codecs offered a tree executable path with Auto-Detect, Browse and a validation tick, and none of it did anything: the value was threaded all the way into the HTML writer, which never read it, and no external `tree` process has been launched since HTML output became native. A control with a tick tells an operator it matters — this one nearly put `tree` into a deployment manifest as a dependency for a binary the apps never call. The section, its plumbing and its now-orphaned validator are removed. An existing stored path is left untouched: still read at launch, never written and never deleted, so nothing is lost and an admin-provisioned value stays readable. Presets were never affected — they deliberately exclude executable paths. |
| **CopyTrust 2.7.2** *(public beta prerelease, Build 32)* | 2026-08-09 | **A card's artifacts could stop dead and spin forever.** In a two-card test the first card's contact sheet wrote one destination's PDF, got partway through the second, and then that card produced nothing for the rest of the session — the row sat on its last message, its proxies sat on "Queued…", and Rebuild All was the only way out. The cause was a metadata subprocess whose output pipes were left undrained, so it could block on a full buffer and neither its own deadline nor the outer timeout could unwedge it; it is now drained continuously and killed if it hangs. A **stall watchdog** backs that up: any artifact silent for five minutes is failed with a per-artifact **Retry**, so nothing spins indefinitely again. **Proxy progress** now shows the work left across the whole run — `7 of 10 · A001 → RAID 01 · 15%` — instead of a per-destination count and a wall of encoder detail. **End Session** no longer did nothing while an artifact was wedged — it waited on a task that could never finish; the wait is now bounded by progress, and while artifacts run the button offers End Now or Wait. **The menu bar** no longer reads as finished while proxies encode: it shows which cards are safe to eject and what is still running. **Session Health and the receipt text** grow with the window instead of sitting in fixed scroll boxes. Includes Build 31. **CopyTrust only.** |
| **CopyTrust 2.7.2** *(public beta prerelease, Build 31)* | 2026-08-09 | **Every card in a multi-card run is visible, and the counts add up.** Staging two cards showed only the one copying right now; the queue row now lists both, each with its own bar and status, and its progress bar covers the whole run instead of filling to 100% at the end of the first card. File counts are **file copies** — one per file per destination — so a 47-file card going to two destinations counts toward 94, not 47, and the menu bar agrees with the activity log. The menu bar also shows which card is running and a second bar for all of them. **CopyTrust only; Drop Verify stays at Build 15, Folder Copy Compare at Build 14.** |
| **Drop Verify 2.7.2** *(public beta prerelease, Build 15)* | 2026-08-08 | **The proxy progress bar no longer sweeps back and forth.** Proxy encoding is the longest phase of a run and had the least readable progress: the artifact phase reported only a message, so the bar fell through to its indeterminate branch for the whole phase, even though ffmpeg's own per-clip percent had been driving the status text since Build 8. That percent now drives the bar too — each clip owns one slice, filled by its encode percent and closed out when the clip finishes, so the bar only moves left to right. A clip whose duration ffmpeg cannot determine holds at the start of its slice and steps forward whole rather than guessing at a position inside it, and a failed clip still closes its slice. **Shipped in the 2.7.2+30 pre-release alongside CopyTrust Build 30 and, for the first time, MHL Verify 2.6.0.** |
| **CopyTrust 2.7.2** *(public beta prerelease, Build 30)* | 2026-08-08 | **One blue action button.** Colour now says what a control does: blue is the action and there is exactly one on screen — `Start`; orange is a mode change that alters what a run will do but starts nothing; neutral is optional or later. The `Simultaneously` / `In series` switch is now **orange**, matching the Card/Folder switch — it carried no tint, so it inherited the blue accent and its selected segment read as the button to press when it only decides what `Start` will do. The Queued Sessions panel **loses its own `Queue Current Session` button**: it was styled as the primary action while explicitly not starting a copy, and it was a strict duplicate of the action-bar button, which remains — the panel now carries the explanation alone, saying where the control is. The Help sheet's topic picker stays blue, being modal navigation rather than a mode. **Drop Verify and Folder Copy Compare stay at Build 14; Build 30 touches CopyTrust only.** |
| **CopyTrust 2.7.2** *(public beta prerelease, Build 29)* | 2026-08-08 | **One Copy switch, one Start button.** With two or more destinations a segmented **Copy** switch chooses **Simultaneously** (every destination at once, the source read once per destination) or **In series** (each stop's verified output feeds the next, the source read once), and one button — always labelled **Start** — runs it. This replaces `Queue Relay Chain`, which appeared three times, started nothing, and left the operator to find `Start Queue` elsewhere: because the pre-copy review fires when a copy starts, a relay chain built that way ran with **no review at all**. Start now builds and starts the chain in one action, so the review always appears. The four contextual Start labels are gone with it. **Fixed: every relay copy crashed just after the first stop finished** — the immutable workflow plan exported into each destination's receipts folder was written with a file-write option pair macOS rejects outright, taking the app down with files, MHL and a completed manifest already safely on disk but the queue not yet updated. **Interrupted jobs now recover:** on launch a job still recorded as running is checked against the destination's own manifests, so a copy that had actually finished comes back complete and the chain carries on from the next stop instead of reopening stuck. **New relay chains** put Archive to P5 on the first stop and proxies on the last — camera archive first, editing storage last. **Drop Verify and Folder Copy Compare are rebuilt at 2.7.2 Build 14 alongside this release for suite alignment only** — neither has changed functionally since Drop Verify Build 8. Testing notes: [TEST_NOTES_v2.7.2.md](TEST_NOTES_v2.7.2.md). |
| **CopyTrust 2.7.2 / Drop Verify 2.7.2 / Folder Copy Compare 2.7.2** *(public beta prerelease)* | 2026-08-07 | **Drop Verify gains proxy media**, using the same generator, codecs (HEVC / H.264), sizes (12.5 / 25 / 50%) and Final Cut folder layout as CopyTrust — off by default, with per-clip progress (percentage, speed, ETA) and its own JSON receipt, readable summary and encode log. Three rules govern it: only files that passed verification get a proxy (so enabling proxies hashes the media even when MHL is off), proxies are written beside the media and are never copied to the export folder because they are footage rather than evidence, and a clip that fails to encode is reported and skipped without changing the verified result. **Sessions now record the operator:** `SessionManifest` carried hostname, macOS and tool version but had no operator field in either app; both now write it, and the operator name defaults to the macOS account when blank so receipts are never anonymous. **Fixes:** "Open Artifacts" did nothing on a proxy-only run; the proxy row linked to one arbitrary movie rather than the folder; the session manifest was never exported on any run; proxies were missing from the history CSV; and the artifact-URL fallback matched exported files by substring, which could report an artifact that was never made. |
| **CopyTrust 2.7.1** *(public prerelease, Build 14)* | 2026-08-07 | **Operator presets:** save every Card **and** Folder setting under one name and load it back in a single action, from a Preset menu beside the copy-mode picker. A dot marks settings changed since loading; "Update … from Current Settings" re-captures in place. A preset carries policy, not environment — both mode profiles plus confirm-before-copy, external codecs, ExifTool, the codec extension lists, receipt export and notifications. Destinations, tool paths, P5 credentials, operator name and appearance are deliberately excluded so a preset stays portable between machines. Shared presets deploy read-only from `/Users/Shared/CopyTrust/Presets`, a path that exists on a fresh Mac and needs no user account, so presets can be placed by script or MDM before an operator's first launch; personal saves go to Application Support so re-deploying can never clobber them. Loading a preset never changes destinations — it names the destination set it expects and says whether that set exists on this Mac. **Naming:** "preset" previously meant two different features, so the Card/Folder toggle is now the copy **mode** and destination presets are **Destination Sets**. |
| **CopyTrust 2.7.0** *(public prerelease, Build 12)* | 2026-08-01 | **Proxy rotation and color correctness:** CopyTrust probes each delivered original before encoding, calculates proxy dimensions from displayed orientation, carries reported color range/primaries/transfer/matrix into the encode, and records encoded/display dimensions, rotation, and source/proxy color evidence. Missing source color fields remain N/A instead of being claimed as preserved. Sorted-destination provenance fallback now records the actual active sort configuration. Includes Build 11 workflow review and immutable relay evidence plus Build 10 per-destination P5/proxy routing. |
| **CopyTrust 2.7.0** *(public prerelease, Build 10)* | 2026-07-31 | **Relay-chain P5 pre-check and per-destination proxies:** P5 selection is evaluated across every queued relay stop, eliminating the false no-destination warning when a later stop is selected. Each destination now has an independent **Create proxies** checkbox, so proxy and P5 routing can target the same destination, different destinations, or both. Presets and queued sessions preserve proxy choices; older saved data remains proxy-enabled by default. Includes the Build 9 proxy-result correction, guarded P5 archive workflow, queue hardening, and privacy-safe Sentry baseline. |
| **CopyTrust 2.6.0 / Drop Verify 2.6.0 / Folder Copy Compare 2.6.0** *(public beta prerelease, Build 1)* | 2026-07-28 | **CopyTrust proxy media beta:** optional H.264 High or HEVC Main 10 MOV proxies at 12.5%, 25%, or 50%; optional dated `Final Cut Proxy Media` layout; exact original basenames for relinking; live clip/total, percentage, speed, and ETA; JSON/TXT/LOG evidence comparing original and proxy technical properties. The pre-copy confirmation and session log explicitly record Proxy Off or the selected settings. Real automated encodes cover MOV and MXF; other inputs depend on packaged ffmpeg decoder support. **Package safety:** CopyTrust and Drop Verify put receipt and proxy folders beside `.fcpbundle` and other macOS package roots rather than inside them. |
| **CopyTrust 2.5.5 / Drop Verify 2.5.5** *(testing, Build 1)* | 2026-07-24 | Field-test follow-up. **CopyTrust:** a copy now shows a **mode-confirmation** summary (mode, verification level, artifacts, destination sort, contact-sheet split) before it starts, so an accidental Folder-mode card copy is caught before it runs — shown only on manual/queued starts, never on auto-advance or resume, and toggleable in Settings → Post-Copy → Before Copy. Each copy start also logs a one-line mode summary, and the activity log pane is now resizable (height remembered across launches). **`.sb-*` sandbox temp files** (created by FCP/Motion) are now skipped silently like any other exclusion — no longer listed as transient skips or shown in the Session Health report — with the count noted once in the log. **Drop Verify:** Contact Sheet / Metadata CSV / Project Tree HTML now generate independently with per-artifact status, individual **Retry**, and **Rebuild All**; retry reuses the completed scan/hash and re-exports when configured. |
| **CopyTrust 2.5.4 / Drop Verify 2.5.4** *(testing, Build 6)* | 2026-07-22 | New **Split large contact sheets** setting in both apps (Off / every 250 / 500 / 1,000 files): large cards and folders are written as numbered PDFs (`…_part1of9.pdf`) with a "Part x of y — files a–b" header line instead of one very large PDF. Fixes the large-card contact sheet timeout: the 180 s limit now scales with file count, a timed-out run stops promptly and leaves no orphaned PDFs, and with auto-open enabled a multi-part result is revealed in Finder instead of opening every PDF. Build 2 (field-test feedback): disabled artifacts log an explicit "disabled in <mode> mode settings — skipped" line instead of silence; contact sheet generation logs heartbeat progress (metadata pass + per-part written lines) and the style/split/timeout in effect; per-mode Post-Copy controls refresh immediately; HTML tree artifacts renamed `tree_…` in both apps (legacy `dropverify_tree_…` still recognized). Build 3: the live activity log gains a copy heartbeat line every ~5 s (percent, file copies, bytes, speed), and the menu bar shows an orange verification bar with its own percentage during post-copy verify instead of a full copy bar. Build 4: fixes a deadlock where contact sheets on proxy (LRF) / MXF-heavy cards hung forever (blocking subprocess read that also defeated the timeout, plus a redundant preview audit pass) — subprocesses are now killable (SIGTERM→SIGKILL) with non-blocking stderr, and the audit is skipped when placeholders are shown / timeout-wrapped when hidden; Contact Sheet / EXIF CSV / HTML Tree each get their own status and Retry. Build 5: artifact filenames reconciled to a subject-first scheme in both apps — `<subject>_<stamp>_<app>_<type>.<ext>` for contact sheet / EXIF CSV / HTML tree (sorts by subject, keeps creator + type in the name); MHL keeps its industry-style name and receipts are unchanged; discovery/export/retry recognize legacy names. Build 6: fixes the critical Quick-verification regression where a successful copy produced no PDF/CSV/tree because descriptive artifacts incorrectly depended on the hash-only file list. CopyTrust now keeps a separate delivered-file inventory for contact sheet, EXIF CSV, HTML tree, destination sort, retry, and receipt counts without inventing hashes; Quick still writes no MHL. Enabled artifact rows now always reach a terminal state instead of spinning forever. |
| **CopyTrust 2.5.3 / Drop Verify 2.5.3** *(stable)* | 2026-06-15 | Current stable release for both apps (promoted 2026-07-19). CopyTrust includes the sorted-copy MHL verification fix, per-copy provenance records, and the no-re-sort reconnect fix. Drop Verify includes exact output toggles, no-hash CSV/contact-sheet modes when MHL is off, and HTML-tree-only runs that skip media analysis. Both apps now generate Project summary index, One HTML per top-level folder, and Entire project HTML trees with built-in file enumeration, so the external `tree` command is no longer required. |
| **2.5.2** *(internal testing milestone)* | 2026-06-13/15 | Pre-2.5.3 testing notes: CopyTrust sorted-copy verification/provenance work and Drop Verify output-toggle/no-hash workflow fixes. Superseded by the combined CopyTrust 2.5.3 / Drop Verify 2.5.3 stable release above. |
| 2.5.1 | 2026-06-12 | ASC MHL v2.0 (Silverstack 9+) read/verify support in the shared CopyCore engine — fixes [#1](https://github.com/macvfx/MHL/issues/1); `ascmhl/` folder layouts; mid-file verification cancellation; mhl-tool version aligned |
| 2.5.0 | 2026-06-09/10 | CopyTrust Session Health Report: structured error classification, per-mismatch explanations, health verdicts in Session Summary, receipts, and logs; per-mode artifact settings |
| 2.4.9 | 2026-06-07 | Folder Copy Compare reference-folder (3-way) compare, saved profiles with folder watch, two-phase scan progress; CopyTrust HTML tree artifact; Drop Verify session history |
| 2.4.8 | 2026-06-06 | Drop Verify HTML directory tree output (ProjectToHTML) with in-app Drop Verify Help; exclusion verification tests; version alignment; Sentry scoped to CopyTrust only |
| 2.4.1 (7) | 2026-05-01 | Copy progress: smallest-first ordering, within-file byte progress; NAS/SMB copy hang fixes; Check All Hashes; Clean Windows Files; Check for Updates in all apps |
| 2.3 | 2026-04-25/29 | Subfolder Check repair workflows, guided symlink recovery, P5 stub cleanup |

## MHL Verify

| Version | Date | Summary |
|---|---|---|
| **2.6.0 (1)** | 2026-08-08 | **Side-by-side compare:** `Compare…` opens `Side A` / `Side B`, each picked separately by choose, drop, or recents, so two MHL files on different volumes can finally be compared — the old single open panel could only return files from one folder. Each side remembers its own last folder and shows its volume, file count, total size and MHL version before comparing; dropping two files fills both sides; `Swap Sides` flips them. **Compare also reports a verdict** and stops calling expected differences failures: timestamps no longer count as content (two copies of one card reported every file as `Changed` because the hash times differed), and a **Destination Sort**ed copy is now paired with its pre-sort source by name, size and hash and reported as `Moved` rather than as every file added *and* removed |
| 2.5.1 (2) | 2026-08-02 | MHL Verify moved to its own release home at [macvfx/MHL-Verify](https://github.com/macvfx/MHL-Verify), so update checks stop pointing at suite releases that carry no MHL Verify build; update-alert dismissal fix |
| **2.5.1 (1)** | 2026-06-12 | ASC MHL v2.0 support via the shared CopyCore engine (fixes [#1](https://github.com/macvfx/MHL/issues/1)) + new **Verify** action that re-hashes files against the MHL; requires macOS 14 |
| 2.4.1 (7) | 2026-05-02 | Check for Updates; version aligned with the suite |
| 0.7 (2) | 2026-04-30 | Reader scroll fix; Editor-role handler registration for both MHL UTIs |
| 0.6 | 2026-03-10 | Initial reader, compare, export, and Handlers utility |

## mhl-tool (CLI)

| Version | Date | Summary |
|---|---|---|
| **2.7.7** | 2026-08-28 | **The manifest is written where its paths resolve, and subfolders survive.** `create` wrote the MHL into a `<name>_Receipts` subfolder while every path inside it stayed relative to the folder *above* — so each entry pointed one level too deep. It went unnoticed because `mhl-tool verify` compensated, which meant create-then-verify worked here and nowhere else: any other vendor's reader resolved every path into a folder that does not exist. The manifest now goes in the folder it describes, beside the files it lists, which is where MHL's convention puts it. No subfolder is created — `create` only ever wrote one file. **A second fault, found proving the first: paths were being flattened.** Building each entry's relative path fell back to the bare filename whenever it could not match the folder prefix, and it could not match under `/tmp`, where macOS reports the same directory two ways — so `Sub/clip.mov` was recorded as `clip.mov`. Two clips sharing a name in different subfolders wrote two identical entries: a manifest that checks one file twice, never looks at the other, and reports a clean pass. A path that cannot be made relative is now an error rather than a guess. **Nothing already written is stranded:** `verify` still finds a manifest in a `Receipts`, `*_Receipts` or `ascmhl` subfolder and still resolves it against the parent, so manifests from earlier versions keep verifying untouched. `--output` is unchanged. Version realigned with the suite. |
| **2.5.1** | 2026-06-12 | Verifies ASC MHL v2.0 manifests (auto-detects `ascmhl/` roots); version aligned with the suite |
| 1.0.0 | 2026-03 | Initial release: create/verify MHL v1.1, media-only or all-files, JSON output, signed pkg |
